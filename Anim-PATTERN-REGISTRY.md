# Anim-PATTERN-REGISTRY — 感受原子注册表

> 性质：架构约束，Anim 类型系统的核心数据库
> 范围：感受原子的验证、收录、版本管理、多物种分区、设备端部署
> 核心：Pattern Registry 是 Anim 的类型系统底座。感受原子不是随便命名的——每一个名字背后是一组经过神经科学验证的独立通路映射。Registry 不做推荐。Registry 只做一件事：你写的这个感受名——在人身上对应一组真实存在的神经信号。不存在 = 交织器拒绝。

---

## 零、Registry 是什么

```
Registry 不是"感受的菜单"。
Registry 是"神经通路的一一映射表"。

    每个感受原子 → 一组神经通路（迷走神经/CT纤维/岛叶/杏仁核/前额叶）
    每组通路 → 一组安全参数（点缀上限、强度上限、创伤分型规则）
    每个安全参数 → 在 Pass 1-3 被逐条检查

编译器和标准库的关系 = animi 和 Registry 的关系。
没有 Registry → animi 不知道"calm_meditative"是什么意思。
```

---

## 一、收录标准——四问

感受原子进 Registry 的标准不是"这个感受好不好"。是这四问：

```
1. 这个感受在人体内有独立的神经通路吗？
    → 有独立通路 → 收录。无论多暗。无论多痛。
    → 没有独立通路 → 不进。不是"不好"——是不存在。
    例：calm_meditative → 迷走神经耳支 + 前额叶α。独立通路。收录。
    例：sad_and_happy_mix → 不存在独立通路。是两种感受的同时激活——应该用 mix 实现。

2. 这个感受的安全参数可以校准吗？
    → 可以 → 收录。点缀上限。强度上限。创伤分型规则。
    → 不可以 → 不进。不是"太危险"——是无法安全参数化。
    例：fear_threat → 点缀上限 0.12。创伤规则明确。收录。
    例：uncontrollable_rage → 缺乏可校准的强度-响应曲线。不进。

3. 删掉这个感受会导致反向感受的对比基底塌陷吗？
    → 会 → 必须收录。不是因为"有用"——是因为少了一极，另一极不是它自己了。
    → 不会 → 还需要看前三条。
    例：删掉 fear_threat → 勇敢的基底塌了。必须收录。

4. 这个感受有跨文化/跨个体的神经一致性吗？
    → 有 → 核心原子。全体用户。全强度区间。
    → 没有/未验证 → 沙盒原子。≤30 强度。创作者本人。不给创伤协议用户。
```

---

## 二、Registry 的物理部署

```
模型：设备本地为主。Server 端为辅。

设备本地（Primary）：
    Registry 核心库随设备固件预装。
    大小 ≈ 数 MB（所有核心原子的神经通路映射 + 安全参数）。
    首次配对时无需下载。
    更新通过固件 OTA。

Server 端（Backup + 分发）：
    沙盒原子托管在 Feelings-Sandbox 仓库。
    用户自愿下载。不在设备预装里。
    核心原子版本升级时——Server 推送更新到设备本地。

离线可用：
    设备不需要网络就能交织。
    Registry 在设备本地 → Pass 1 TypeCheck 零网络延迟。
    这是 Anim-LOCALITY.md 里"Pattern Registry 按共现频率分桶存储"的前提——
    Registry 物理上在设备本地。不是在 Server 端每次查询。
```

---

## 三、维护者与验证流程

```
核心原子的增删改：

    维护者：Feelings 核心团队 + 外部神经科学顾问。
    新增核心原子需要：
        1. 独立神经通路的文献证据（至少一篇 peer-reviewed 论文）。
        2. 安全参数的校准数据（强度-响应曲线、创伤分型规则）。
        3. 至少两名独立审核人通过。审核人不能是提案者本人。

    删除核心原子：
        不可删除。只能标记为 deprecated。
        已编译的 .anim 文件如果引用了 deprecated 原子 → Pass 1 警告。不报错。

    修改安全参数：
        需要更新的神经科学证据。
        至少两名独立审核人通过。
        修改后 → 已编译的 FSIR 缓存全部失效。下次 Session 启动重新生成。

沙盒原子的增删改：

    维护者：创作者本人。
    基础格式校验 + 恶意注入扫描 → 自动通过。
    无完整安全性验证。
    创作者自己对沙盒原子的安全负责（见 ADR 005 宏的创伤作用域）。
```

---

## 四、版本管理

```
Registry 版本号：semver。MAJOR.MINOR.PATCH。

    MAJOR：删除或大幅修改核心原子的安全参数。
    MINOR：新增核心原子。新增沙盒原子分类。
    PATCH：参数微调。文档修正。

设备端缓存策略：
    设备固件 OTA 时更新 Registry。
    用户也可手动刷新（从 Server 拉最新版本）。
    .anim 文件声明最低 Registry 版本：
        @requires_registry >= 1.3.0
    Pass 1 检查：设备本地 Registry 版本 < 要求版本 → 编译拒绝。
```

---

## 五、多物种分区

```
Anim-FEELINGS.md 说感受不只人有。万物皆有感受。
Registry 按物种分区存储：

    human/      人类神经通路映射（完整）
                迷走神经耳支 + CT纤维 + 岛叶 + 杏仁核 + 前额叶
    canine/     犬类神经通路映射（预留）
    feline/     猫类神经通路映射（预留）
    ai/         具身AI载体（心跳模拟/皮电模拟/呼吸模拟通路）

同一个感受名（accomplishment_certainty）——
    在 human/ 下和 canine/ 下是不同的神经通路、不同的安全参数。
    Pattern Registry 不跨物种混存。
```

---

## 六、和已有文档的咬合

```
本文                                    Anim-PATTERN-REGISTRY——感受原子注册表的设计与维护
Anim-FEELINGS.md                        感受全谱——为什么极端感受必须收录。
                                        收录四问 = 本文 §一。
Anim-PHILOSOPHY.md                      双原子体系——核心原子 + 沙盒原子。
                                        Registry 的区分存储 = 同一个哲学在数据层的实现。
ADR 003: Pass 管线                       Pass 1 TypeCheck —— 每个感受标识符查 Registry。
                                        不在 Registry → 编译拒绝。
Anim-LOCALITY.md                         Registry 设备本地存储 → 零网络延迟。
                                        按共现频率分桶 → 查询命中率最优。
GOVERNANCE-FEELINGS.md                   Feelings-Core 私有层——Registry 的具体神经通路映射
                                        属于私有层。不开源。这是安全边界的物理保证。
```
