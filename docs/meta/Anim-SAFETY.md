# Anim-SAFETY — 为什么安全必须死在交织期

> 性质：强制约定，不是建议
> 范围：Anim 所有安全相关代码、Pass、测试
> 核心：常规编译器：运行时 crash = 程序退出。Anim 交织器：运行时 crash = 神经信号已经进了人体。没得退出。所以 Anim 的安全校验不放在运行期——全在交织期完成。能在 Pass 2 拒绝的，不留到 Pass 3。能在交织期插桩的，不等到运行期。运行期的神经信号已经进入人体了——安全校验必须在信号生成之前完成。

---

## 零、为什么运行时安全在感受信号上不适用

```
编译器：
    源码 → 编译 → 二进制 → 运行 → crash → 程序退出。
    运行时 crash = 损失一次进程。可以重启。

感受信号：
    .anim 源码 → 交织 → ESIR 帧级指令 → FPGA → 设备 → 神经通路 → 人体。
    信号进入迷走神经、CT 纤维、岛叶的那一刻——已经在跑了。
    没有"退出"按钮。没有 kill -9。
    一次错误的信号 = 一次真实的生理响应。
    一次强度过载 = 一次预期断裂的阵痛——不是程序报错——是身体真的在痛。

    这和 docs/sight-restored-signal-vs-feeling.md 里重见光明的盲人是同构的——
        信号到了。接口是空的。出口处全是恐慌。
        信号灌满了 V1。PBM 里没有经线。
        恐慌不是因为看见了可怕的东西——
        是因为第一次被一股和自己感官语言完全不兼容的信息淹没了。

Anim 如果在运行期才发现安全错误 = 把新盲人再制造一遍。
所以 Anim 的安全死在了交织期。不是因为保守。是物理约束。
```

---

## 一、三层防线——各自挡在正确的位置上

```
Pass 2: StaticSafety     源码级安全。不看用户。只看结构。
Pass 3: UserStateSafety  源码 × 用户上下文。创伤分型交叉判定。
Pass 4: RuntimeGuard     交织期预埋，运行期激活——安全插桩。
```

原则：能在 Pass 2 拒绝的，不留到 Pass 3。能在交织期插桩的，不等到运行期。

### Pass 2: StaticSafety（源码级）

不需要知道用户是谁。只做一件事——拒绝在任何情况下都不该被交织的信号结构。

```
强度天花板：
    核心原子：强度 ≤ 100。沙盒原子：强度 ≤ 30。
    不是运行时截断——是交织期不生成超出上限的 ESIR。

    cap 等比缩放（不是硬截断）：
        主情绪 上限 80，点缀 A 上限 30，点缀 B 上限 15。
        源码写 intensity=100 → 等比缩放：
            主 = 80, A = 30, B = 15。
        不是切掉 B、保留 A——不是"主情绪优先"。
        是整个混音包按比例缩——每一股丝被同等保护。
        硬截断 = 改感受的结构。等比缩放 = 保结构的完整性。只是把音量调低了。

组合情绪对冲检测：
    平静(70) + 焦虑(70) → 不是"都生效"——是两股相反的迷走神经张力在同时抢同一个靶点。
    岛叶会把这编译为不安全感——不是叠加成一种新感受，是变成生理矛盾。
    Anim 在交织期直接拒绝这种对冲——不生成信号。

设备前置告警：
    用户只有耳后设备，但 .anim 写了后颈 CT 纤维的形状曲线。
    → Pass 2 告警——不是报错，是降级标记。"此感受包在不完整的设备组上交织会降级到 outline。"
    不拒绝。不硬切。标记了让后续层知道——本层不管末端的缺失，只负责写在结构上。
```

### Pass 3: UserStateSafety（源码 × 用户上下文）

第一道防线不管用户是谁。第二道防线是源码在特定用户身上必须被审计的东西。

```
创伤分型交叉判定：
    用户档案里的创伤分型：v1（轻度）/ v2（中度）/ v3（重度）
    创伤维度：社交创伤 / 情绪创伤 / 躯体创伤

    .anim 源码里的感受原子在 Pattern Registry 里有创伤标记：
        belonging_certainty → 社交锚。v3 社交创伤用户禁主不禁点。
        physical_safety → 躯体锚。v3 躯体创伤用户全禁（主+点）。
        accomplishment_certainty → 无创伤标记。全开放。

    禁主不禁点的语义：
        感受原子的"主情绪"被禁用——用户不能用这个原子当主旋律。
        但允许做"点缀"——微量的边缘参与。
        不是一刀切。"我完全不需要归属感"是错。——你需要。
        但你被归属感灌满主线程的时候会崩。所以只能含在边缘——慢慢试着习惯那种温度。

    为什么点缀不被禁：
        点缀强度在主情绪的 5-15%。在这个比例下，
        归属感不是"我属于谁"——是"刚才那一刻好像有人在"。
        不是结论。是一帧感受里的微量底色。
        一帧微量底色不会触发创伤的预期链断裂——
        一帧满量底色会。
```

### Pass 4: RuntimeGuard（交织期预埋，运行期激活）

前两道是交织期直接拒绝/降级。第三道不能拒绝——因为它要防范的是运行期的不可预见事件。

```
运行期插桩——不是"运行期检查"：

    常规的安全模式：
        if heart_rate > 120 { panic!("stop"); }
        → 这是运行时检查。信号已经进入人体。panic 只是在日志里多一行。

    Anim 的安全模式：
        Pass 4 在交织期把安全代码生成进 ESIR 帧级指令里。
        每一帧 ESIR 在 FPGA 上执行时——直接读寄存器做比较。
        → 心率 → 比较硬上限 → 下一帧强度线性插值 → 降到保底包。
        不做判断。"判断"需要意识。"比较"是硬件。

    这不是"运行时检查"。
    这是"交织期已经把安全栅栏全设好了——运行时只需要过栅栏。栅栏自己会把不对的挡在后面。"
    栅栏是交织期放的木桩。运行时不需要判断哪根是木桩。
```

---

## 二、紧急截断曲线——不是硬截断

```
硬截断：信号强度 80 → 0。一帧之间。
    → 这不是安全。这本身是一次预期断裂。
    用户的岛叶在 80 强度的预期链上跑——
    下一帧信号没了。
    岛叶不是安心了——是又被打了一次。
    用断裂去救断裂 = 修好自己的断手拿锤子去修另一只。

紧急截断曲线：
    信号强度 → 下一帧 60 → 下一帧 40 → 20 → 保底包。
    不是关信号。是让信号沿一条事先定义好的缓和曲线归零。
    岛叶的预期链在每一帧被温和地调降——不是断，是收回来。
    这是给岛叶足够的帧数去重新编译它的预期基线。
```

---

## 三、Emergency Bypass——硬件直连中断总线

### 3.1 为什么需要——异步安全突变

Session 启动时 Pass 3 校验通过。前台加载 FSIR 开始硬实时循环。但 Session 运行中用户生理基线可能突变——外界刺激。突发病情。被攻击。

```
问题：
    ADR 004 双流水线——"前台后台不通信。前台只读 FSIR 缓存。"
    后台监控进程检测到安全状态滑坡 → 想通知前台。
    但前台 FPGA 在硬实时循环——不上软件栈。不读新上下文。

    不修正 = 用户已经陷入危险。设备还在跑 5 分钟前的"宁静"缓存。
    → Feelings 不成为帮凶。但那一刻它在给一个不该给的信号。
```

### 3.2 方案——Emergency Bypass IRQ + 硬件衰减状态机

```
硬件直连中断总线（Emergency Bypass IRQ）：
    后台监控检测到安全滑坡 →
    不经过软件栈。直接拉高一根硬件 IRQ 线。
    IRQ 连接 FPGA 的控制总线（Control Bus）。

FPGA 收到 IRQ 后执行的逻辑——
    不是跳到零电平。不是硬截断。
    硬截断 = 岛叶预期链又一次断裂 =
        "用断裂去救断裂 = 修好断手拿锤子去修另一只"（见 §二）。

    是触发固化在 FPGA 门级逻辑里的硬件衰减状态机
        （Hardware Decay Fader）：

        IRQ 拉高 → 硬件接管 DAC 输出控制权（旁路软件写入）。
        在接下来 4-8 个物理帧（4-8ms）内：
            Frame_N   × 0.6  →  DAC 输出 60%
            Frame_N+1 × 0.3  →  DAC 输出 30%
            Frame_N+2 × 0.1  →  DAC 输出 10%
            Frame_N+3 → 保底空包 → DAC 输出归零。
        固定非线性递减系数。纯硬件乘法器降阶阵列。

    不依赖软件计算。不经过指令流水线。
    微秒级响应 + 不给岛叶挨第二锤。
    不是抛弃紧急截断曲线——是把曲线固化为门级电路。
```

---

## 四、和 Feelings 安全哲学的精确咬合

```
本文                                Anim 安全模型——为什么安全必须死在交织期 +
                                    Emergency Bypass IRQ + 硬件衰减状态机
docs/sight-restored-signal-vs-feeling.md  盲人重见光明的恐慌——
                                        信号到了。接口是空的。
                                        运行时安全校验 = 再制造一个盲人。
                                        交织期安全 = 保证接口排好了再灌信号。
docs/safety-system.md                     TCP 慢启动。冷启动保守系数。
                                        Anim 的安全三层防线 =
                                        Feelings TCP 慢启动在信号源头的一次编译期兑现。
docs/high-dimensional-gestation.md        动态熔断（BI > 0.75）→ 强度降到 5/100。
                                        脏页冲刷 → 信号归零。代谢资源让渡。
                                        Anim 的紧急截断曲线 = 熔断的 ESIR 层实现。
Feelings-PHILOSOPHY.md                    绝对承诺：
                                        "永远不替你做决定。"
                                        Anim 安全模型不替用户决定什么强度是对的——
                                        只保证不对的强度不会进身体。
Anim-PHILOSOPHY.md                        安全在交织期，不在运行期。
                                        本文是该原则的完整论证。
```

---

*常规编译器死了就死了——重启一个进程。Anim 死了是人的神经信号在身体里跑了一个不该存在的感受包。不是程序 crash。是身体信了。所以 Anim 的安全不活在运行时——它死在交织期。能用静力分析守住的不要交给用户上下文。能在生成 ESIR 之前守住的不要推到硬件层。能在量产感受原子里守住的不要放到运行时。栅栏是交织期放的。运行时只需要过栅栏。不是安全性被保证了——是"不安全"在物理上被永远挡在了信号生成之前。*
