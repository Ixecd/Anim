# Anim-CROSS — 交叉、织入、回退与绳结

> 性质：架构基础，Anim 数据交互的一等操作模型
> 范围：所有 IR 层、Pass 管线、FPGA 帧级执行
> 核心：Rust 有借用、引用、地址、解引用。Anim 不需要这些。Anim 的数据在织机上——不在内存堆里。丝与丝之间不借任何东西。两根丝在同一个坐标相遇——交叉——交换信息——然后各自继续自己的方向。Anim 的一等操作不是 `&x`。是 `cross(warp, weft)`。

---

## 零、Anim 不是内存系统——是织机

```
Rust：
    值在堆上。值在栈上。值有 owner。owner 可以借出去。
    &T → 借用。*T → 解引用。0x7ffe1234 → 地址。
    这是 RAM。随机访问。一切都可以被取址。

Anim：
    数据不在随机访问的堆上。数据在织机上。
    一根丝 = 一股独立的信号流。
        丝有自己的纺锤（Spindle）——产生它的源。
        FSIR 的纺锤 = .anim 源码
        PSIR 的纺锤 = 个人基线矩阵
        DSIR 的纺锤 = 设备集合
        ESIR 的纺锤 = 实时回读环

    丝在时间轴上移动。不同纺锤出的丝在不同的方向上、以不同的密度运行。
    两根丝相遇 = 交叉（Cross）——不是一方借另一方——是双方同时到达同一个坐标。
    交叉之后——两根丝继续各自的轨迹。
    经线还在。等下一根纬线来交叉。
    纬线完成交叉——退场。不保留。下一帧是新纬线。
```

---

## 一、交叉（Cross）——Anim 的第一等操作

```
Rust：
    let x = &y;   ← 借用。x 依赖 y 存在。
    *x            ← 解引用。拿到 y 的值。

Anim：
    warp cross weft at (pass=6, frame=N);

    交叉 = 两根独立丝在同一个时空坐标相遇。
    交叉点（Crossing Point）不是一根丝引用另一根丝的结果——
    是两根丝同时在场——共同产生了一个值。

    warp:  "此人的平静 = 迷走神经 0.4mA"          ← FSIR × PBM 产生
    weft:  "上一帧心率偏了 +2bpm，需要调到 0.41mA" ← 闭环偏差产生
    cross: 交叉点 = 0.41mA。                         ← 不是谁借用谁，是共同输出

交叉之后：
    warp 继续向前。同一个 Session 内不变。等下一帧的 weft 来交叉。
    weft 退场。这一帧的偏差量已经用完了。下一帧是新的 weft。
```

### 交叉的类型

```
确定性交叉（Deterministic Cross）：
    两根丝都已知。坐标已知。交叉点可精确计算。
    FSIR × PBM → PSIR。Pass 6。
    交叉发生一次。不反复。

帧级交叉（Frame Cross）：
    一根丝固定（设备参数）。一根丝每帧都换（生理偏差）。
    DSIR × (偏差修正量) → 下一帧 ESIR。
    Pass 8。每 1ms 交叉一次。

安全交叉（Safety Cross）：
    一根丝是安全阈值。一根丝是当前生理读数。
    阈值 × 当前读数 → 安全判定。
    Pass 4 预埋。ESIR 帧级。每 N 帧交叉一次。
    交叉结果 = 放行 或 切回保底包。
```

---

## 二、织入（Weave）——纬线穿过整排经线

```
一根 weft 穿过所有经线 = 一路产生交叉点 = 交叉点连成线 = 织入。

例：一个 Session 的生命周期

Session 启动——
    weft = 用户请求 session 的那一刻产生的初始信号。
    穿过经线 → FSIR（感受结构）→ PBM（个人基线）→ DeviceSet（设备组合）
    → 每个交叉点产生一段信号 → 连成一条完整的 ESIR 帧。

Session 运行中——
    每 1ms 一根新 weft（偏差修正量）。
    穿过同一排经线。
    一路产生新交叉点 → 新 ESIR 帧。
    weft 变了。经线没变。交叉点每帧微调。

Session 结束——
    最后一根 weft 退场。
    经线还在。下一个 Session 有新的 weft 来。
```

---

## 三、回退（Uncross）——撤销交叉

```
Rust：
    drop(x);   ← 值离开作用域。内存释放。所有权收回。

Anim：
    uncross(warp, weft, at);

    不是内存释放。是交叉点被撤销。
    安全停止帧触发 → 当前帧已经交叉但还没输出的 weft → uncross。
    交叉点不在 ESIR 中产生任何值。
    经线没有因此损坏。没有一根丝因为 uncross 而断。

回退 ≠ panic。回退 = 这一帧的交叉不生效。
    经线还在。下一帧正常交叉。
```

---

## 四、绳结（Knot）——被验证过的交叉路径

```
Rust 的地址 = 这个值在哪。0x7ffe1234。
Anim 的绳结 = 这根丝上次和谁交叉过。在哪一层。在哪一个 Session。

不是：
    "PBM 数据在地址 0x7fff5678"

是：
    "PBM 和 FSIR 在 Pass 6 Personalize 交叉过一次。
     上一次交叉是 Session 47——心率恢复斜率在半夜触了 BI 熔断。
     那一次交叉点的坐标在 Pass 8 CodeGen 的紧急截断曲线里。"

一根丝的"地址" = 它和哪些其他丝交叉过的全部历史。
不是内存里的位置。是在整张交织网上的位置。
```

### 绳结的作用——加速下一次交叉

```
同一根 warp（PBM）和同一根 warp（FSIR）之前交叉过 47 次。
每次交叉产生的交叉点被记录为绳结。
第 48 次 Session 的 weft 来交叉时——
    先找到最近的绳结。
    不用重新从零开始交叉。
    绳结附近偏差小 → 快速通过。
    绳结附近偏差大 → 需要重新计算交叉路径 → 但绳结仍然给出了起始位置。
```

---

## 五、交叉检查器（Cross Checker）

Anim 不需要 Rust 的 borrow checker——Anim 需要 cross checker。

```
borrow checker：
    &mut 不能同时存在两处。
    所有者不能在其被借用期间移动值。
    → 防止 use-after-free。data race。内存未定义行为。

cross checker：
    一根不能和创伤原子交叉的经线——永远不会有和创伤原子交叉的点。
    一根丝不能和强度超过 cap 的 weft 交叉。
    一根丝不能在经线未完全加载前开始交叉。

不是内存冲突。是线冲突。
不是"你借了别人就借不到了"。
是"这根丝没有和这根丝交叉的资格"。
```

---

## 六、和已有架构的精确咬合

```
Anim-CROSS                              交叉模型——数据交互的全部哲学基底
ADR 002: 四层 IR                       每层 IR = 一根独立的 warp。
                                        层与层之间不是"传递数据"——是交叉。
ADR 003: 八 Pass                       Pass = 交叉发生的坐标。
                                        Pass 6 = FSIR_warp × PBM_warp 的确定性交叉。
                                        Pass 8 = DSIR_warp × deviation_weft 的帧级交叉。
ADR 004: 双流水线                      前台 = 帧级交叉（每 1ms 一根新 weft）。
                                        后台 = 确定性交叉（warp-to-warp，一次完成）。
Anim-LOCALITY.md                       绳结 = 时间局性的结构化表达。
                                        最近的交叉点留在 cache 里 = 下次交叉更快。
Anim-SAFETY.md                         cross checker = borrow checker 不是同一层——
                                        但同样在"错误发生之前阻止它"。
                                        只是阻止的是交叉，不是借用。
ADR 005: Anim 宏系统                   macro_rules! 展开 = 丝还在纺锤上的时候被编排。
                                        不是在交叉期重写丝——是纺锤出丝之前已经排好图样。
```

---

*Anim 不需要堆。不需要栈。不需要 0x7ffe1234。Anim 的数据是丝——从纺锤来，在织机上交叉，在绳结上找到上一次的相遇。两根丝各走各路。交叉的那一刻共同产生一个值。不是借用——不欠。不是引用——不指。不是解引用——不在内存里翻地址。Anim 的一等操作只有一个：cross(warp, weft) at time T。纬线退场。经线保持。下帧新纬线再交叉。一帧不多。一帧不少。丝不断。*
